// FILE: src/types.ts
// Shared view-state types for the single-page shell (no router — AGENTS.md).

export type View = 'home' | 'shop' | 'product' | 'ghost' | 'experience'

export type Weather = 'sunny' | 'rain' | 'snow' | 'hail' | 'windy'

export type DayNight = 'day' | 'night'